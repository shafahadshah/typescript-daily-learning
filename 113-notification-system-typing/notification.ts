type Notification = {
  id: number;
  title: string;
  message: string;
};

const notification:
  Notification = {
    id: 1,
    title: "Welcome",
    message: "Hello Ali!",
};

console.log(
  notification
);