const emailInput =
  document.querySelector(
    "#email"
  ) as HTMLInputElement;

const passwordInput =
  document.querySelector(
    "#password"
  ) as HTMLInputElement;

console.log(
  emailInput.value
);

console.log(
  passwordInput.value
);